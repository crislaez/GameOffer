import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.gameOffer',
  appName: 'gameOffer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
